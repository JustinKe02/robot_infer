# 优化版运行配置

当前提供一个锁定 checkpoint 的策略服务配置：

- `step_015900`：完成 15 个 epoch 的 checkpoint（`15900/15900`），相机配置为 `head_right`，
  模型边界为 model16，机器人线协议边界为 raw18。

该配置在加载模型前校验 checkpoint 完整性，并拒绝覆盖策略路径。它只启动回环地址上的策略服务，
不会自行创建机器人或动作传输。真机运动使用已经审计过的旧版机器人客户端连接优化策略服务，
完整命令见 [真机运行手册](../REAL_ROBOT_RUNBOOK.md)。
